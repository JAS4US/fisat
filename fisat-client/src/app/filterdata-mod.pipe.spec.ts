import { FilterdataModPipe } from './filterdata-mod.pipe';

describe('FilterdataModPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdataModPipe();
    expect(pipe).toBeTruthy();
  });
});
